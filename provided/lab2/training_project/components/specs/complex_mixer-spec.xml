<!-- This is the spec file (OCS) for: complex_mixer
     Add component spec attributes, like "protocol".
     Add property elements for spec properties.
     Add port elements for i/o ports -->
<ComponentSpec>

  <Property name="enable" type="Bool" writable="true" default="true"></Property>
  <Property name="phs_inc" type="Short" writable="true" default="-8192"></Property>
  
  <Port name="in" protocol="iqstream_protocol.xml"></Port>
  <Port name="out" producer="true" protocol="iqstream_protocol.xml"></Port>

</ComponentSpec>
